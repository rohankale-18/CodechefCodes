#include<bits/stdc++.h>
using namespace std;
#define int long long int
#define mod 1000000007
#define endl "\n"
#define fio ios_base::sync_with_stdio(false); cin.tie(NULL);

int32_t main(){
    fio;
    int q;
    cin>>q;
    while(q--){
       int n;
       string s,t,u;
       cin>>n>>s>>t>>u;
       char a=s[0];
       char b=t[0];
       int anum=(int)a;
       int bnum=(int)b;
       int inc=abs(anum-bnum);
       for(int i=0;i<u.size();i++){
        u[i]=u[i]
       }
    }
    return 0;
}